#!/bin/sh

while true
do
	current_date="$(date +'%R %F')"
	battery_capacity="$(cat /sys/class/power_supply/BAT0/capacity)/100"
	battery_status="$(cat /sys/class/power_supply/BAT0/status)"
	xsetroot -name "$battery_capacity $battery_status $current_date"
	sleep 1m
done
